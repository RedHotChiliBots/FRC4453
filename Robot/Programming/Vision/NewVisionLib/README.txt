This is the main vision library.
It doesn't do any vision processing on its own, but it instead defined utility classes
	that represent things like targets, points, etc.
Anything wanting to use the vision code will have to include this as a library.